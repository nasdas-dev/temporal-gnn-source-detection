# C code for the IBA algorithm on temporal networks

**Note:** The iba-folder is and adaption of the tsir code of this repository.

To get it running you need:

1. Make a directory o (for complier object files)
2. Compile it by gcc by typing `make`.
3. Run it via a Python wrapper.

